# Checkpoint: 2026-01-29_1434

## Repo state
- branch: main
- head: e7fb5e9

## Working tree
```
(clean)
```

## What changed
- Merged PR #1 (Milestone 1): LA 311 ingestion + raw partitions
- Implemented ingestion package under `src/ingest/`:
  - Env-var config (`LA311_API_BASE`, `LA311_APP_TOKEN` optional, `LA311_RAW_DIR`, `LA311_PAGE_SIZE`)
  - Socrata client using `requests`
  - Writer outputs `data/raw/date=YYYY-MM-DD/la311_YYYYMMDD.jsonl.gz`
  - Idempotency via atomic overwrite (write `.tmp` then rename)
  - CLI supports `--date` and `--start/--end` range plus `--limit` for smoke tests
- Updated verification so `make verify` includes a lightweight ingestion smoke run

## Verification
- `make verify` PASSED on main (2026-01-29)
- Smoke example:
  - `python -m src.ingest.cli --date <YYYY-MM-DD> --limit 10`
  - Output: `data/raw/date=<YYYY-MM-DD>/la311_<YYYYMMDD>.jsonl.gz`

## Next steps
Milestone 2 (Airflow local + ingestion DAG):
1) Add local Airflow via docker-compose (Airflow UI + scheduler + webserver)
2) Create an Airflow DAG that runs ingestion daily (default: yesterday; allow override for tests)
3) Add “DAG parses” (and/or a minimal `airflow dags test`) to `make verify`
4) Document: how to start Airflow + trigger the DAG (README)
