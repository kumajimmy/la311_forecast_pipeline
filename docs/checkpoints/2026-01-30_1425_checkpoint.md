# Checkpoint: 2026-01-30_1425

## Repo state
- branch: milestone2-availability-airflow-plan
- head: (pending commit)

## What changed
- Created `src/ingest/availability.py` with `check_latest(year)` — queries Socrata for max(createddate), falls back to previous year if current year empty, handles network errors gracefully
- Extended CLI (`src/ingest/cli.py`):
  - `--latest` flag (mutually exclusive with `--date`) — ingests latest available date(s)
  - `--days-back N` — with `--latest`, ingests N-day rolling window
  - `--on-empty {fail,warn,skip}` — controls behavior when no records found (default: warn)
- Added 9 new tests: availability (success, empty, network error), CLI --latest (single, days-back, no-data), mutex validation, --on-empty (fail exits 1, skip exits 0)
- Target daily command: `python -m src.ingest.cli --latest --days-back 7 --on-empty skip`

## Verification
```
21 passed in 0.08s
ingest smoke ok (5 lines)
VERIFY DONE
```

## Next steps
- Plan Milestone 3: staging/mart transforms (dbt or plain SQL)
- Airflow DAG scaffolding (wire the daily command)
- Forecast pipeline scaffolding (train/score)
